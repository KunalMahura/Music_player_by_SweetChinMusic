# Creating the YAML content and saving it to a file

yaml_content = """
project:
  name: SweetChinMusic Player
  description: A stylish web-based music player UI.
  version: 1.0.0
  author: Sweet Chin Music Team
  structure:
    - index.html: Main HTML layout of the music player
    - style.css: Styling and layout with responsive design
    - script.js: JavaScript for sidebar menu toggle
  assets:
    fonts:
      - https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700;900&display=swap
    icons:
      - https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css
    images:
      - 1287161.png
      - download.png
      - download (1).jpeg
      - download.jpeg
      - download (2).jpeg
      - download (3).jpeg
      - download (4).jpeg
  scripts:
    - script.js
  stylesheets:
    - style.css
  responsive: true
  dependencies:
    - Boxicons
    - Google Fonts
  features:
    - Sidebar navigation
    - Trending music display
    - Genre-based categorization
    - Music playlist and controls
    - Responsive design
    - Dynamic sidebar toggle
"""

# Save the YAML content to a file
yaml_file_path = "/mnt/data/project.yaml"
with open(yaml_file_path, "w") as file:
    file.write(yaml_content.strip())

yaml_file_path

